# Options pour model_name :
# - "bert-base-uncased"
# - "bert-large-uncased"
# - "roberta-base"
# - "roberta-large"
# - "albert-base-v2"
# - "distilbert-base-uncased"
# - "google/flan-t5-base"  (avec model_type: "flan-t5")
# - "meta-llama/Llama-2-7b-hf"  (avec model_type: "llama")
# - "mistralai/Mistral-7B-v0.1"  (avec model_type: "mistral")

model_type: "bert"
model_name: "bert-base-uncased"  # Modèle spécifique à charger depuis HuggingFace
architecture: "encoder"

# Model details
hidden_size: 768
num_attention_heads: 12
num_hidden_layers: 12
max_length: 512

# LoRA Configuration
lora:
  enabled: true
  r: 8
  lora_alpha: 16
  lora_dropout: 0.1
  target_modules: ["query", "value"]

# Training
learning_rate: 2e-5
batch_size: 16
num_epochs: 10
gradient_accumulation_steps: 2

# Evaluation